FROM python:3.6

WORKDIR /app

ENV RECLADA_RABBITMQ_HOST="localhost"

RUN pip install -U pip
COPY shell_manager/requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

ENTRYPOINT ["/run_shell_manager.sh"]
CMD [ "python", "job" ]
